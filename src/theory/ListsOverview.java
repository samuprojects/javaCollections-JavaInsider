package theory;

@SuppressWarnings("ALL")
public class ListsOverview {

    /*
    * Dentro da API de coleções do Java as listas são representadas pela interface List
    *
    * Lembrando que cada tipo de coleção tem uma interface específica
    *
    * Características
    * As listas podem ter elementos duplicados, tem elementos indexados e os elementos são mantidos em ordem de inserção (exceto se a lista for reordenada)
    * as principais classes usadas para representar as listas são o ArrayList (mais dinâmico que um array comum) e LinkedList (que utiliza um...
    * ... conceito de nós para apontar para o próximo nó que contem os elementos, onde acaba quando o próximo apontamento é null).
    *
    * entre eles o LinkedList em relação ao ArrayList é mais custoso para encontrar um índice pois vai exigir varrer a lista para identificar os índices
    *
    * */
}
